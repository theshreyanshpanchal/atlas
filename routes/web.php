<?php

use Illuminate\Support\Facades\Route;

Route::prefix('atlas')->group(function () {

    // Route::get('/', function () { return view('atlas::layouts.splash'); });

    // Route::get('/docs', function () { return view('atlas::layouts.docs'); });

});
